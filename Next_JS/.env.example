# Copy to .env.local and fill in values

# Redis — must point to the same instance used by the Go proxy
REDIS_URL=redis://localhost:6379

# Must be identical to the Go proxy's SESSION_SECRET
# Used to verify the HMAC signature on the _fpsid cookie
SESSION_SECRET=change-me-in-production
